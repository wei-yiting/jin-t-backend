services:
  web-server:
    build:
      context: .
    env_file:
      - .env
    ports:
      - 8003:10000
    volumes:
      - ./app:/code/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 10000 --reload
